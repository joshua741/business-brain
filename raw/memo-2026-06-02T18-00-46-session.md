**Session summary ΓÇö /watch plugin yt-dlp fix**

What happened: troubleshooting the `/watch` video-analysis plugin, which was failing on YouTube downloads with 403 errors. Root cause identified and the fix recipe was saved to the `watch-plugin-setup` memory.

Key takeaways:
- The current install is stable `yt-dlp 2026.03.17` ΓÇö the exact build that chokes on YouTube's SABR challenge and throws 403s.
- Fix recipe (4 steps): install nightly yt-dlp with EJS support + Deno, and pass `--cookies-from-browser chrome`.
- Caveat noted in memory: those install commands are Unix-style (`curl | sh`, `~/.local/bin`, `ln -sf`) ΓÇö they run in WSL/git-bash, **not** native PowerShell.

No wiki action needed. This is pure tooling/infrastructure, not business/personal/finance context ΓÇö it doesn't map to any wiki page type (entity/project/concept/person/source), and the actionable fix is already captured in the `watch-plugin-setup` memory where future sessions will pick it up. Nothing to add to `wiki/index.md` or `log.md`.

If you'd rather I still log tooling fixes like this somewhere in the wiki (e.g. a `tooling` reference page), say the word and I'll set up that convention.
