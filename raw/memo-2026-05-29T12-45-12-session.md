Saved the Notion API limitation to memory so we skip the dead end next time.

**Your move:** create the teamspace + add Mostafa in the UI (under a minute total). Reconnect Notion via `/mcp`. Then say the word and I'll build the full daily scheduler inside it.
