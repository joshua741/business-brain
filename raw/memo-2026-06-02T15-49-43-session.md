It looks like this is running in the automated summarizer context where I can't get interactive approval or write access ΓÇö so I'll leave it at the summary above.

The setup is effectively complete: `/watch` is installed and both ffmpeg and yt-dlp are verified working. The only open item is the terminal restart to refresh PATH, then the live `/watch` test. The `watch-plugin.md` wiki page is drafted and ready ΓÇö next time you're in a normal session, just say "save the watch plugin wiki page" and I'll write it in.
